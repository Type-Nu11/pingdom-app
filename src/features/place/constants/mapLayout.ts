export const BASE_SCREEN_WIDTH = 430;
export const BASE_SCREEN_HEIGHT = 932;
export const BASE_SHEET_EXPANDED_HEIGHT = 650;
export const BASE_SHEET_COLLAPSED_VISIBLE_HEIGHT = 154;
export const ACTION_BOTTOM_GAP = -6;

export const clamp = (value: number, min: number, max: number) => Math.min(Math.max(value, min), max);
